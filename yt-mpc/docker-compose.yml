version: '3.8'

services:
  dj-sample:
    build: .
    ports:
      - "4000:4000"
    volumes:
      - ./server/downloads:/app/server/downloads
    environment:
      - NODE_ENV=production
      - PORT=4000
    restart: unless-stopped
